node_modules/
dist/
coverage/
*.local

# Secrets and keys — never commit these
*.pem
*.key
*.env
.env.*
!.env.example

# Database dumps
*.sql
*.dump
*.pgdump
!migrations/*.sql
backups/
logs/

# Real user data exported for scripts/migrate-from-d1.ts — password
# hashes, session tokens. Never commit.
d1-export/

# Uptime watch: where alerts go (may hold a webhook URL / API key) and its runtime state
scripts/alerts.json
logs/
.tmp-sup/
logs/
# a local-only helper, never committed
scripts/delete-test-accounts.mjs
sbom.cdx.json
openapi-out/
.oa_tmp/
